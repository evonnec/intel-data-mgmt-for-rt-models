CREATE TABLE raw_all_trades_predictors (date_time timestamp without time zone NOT NULL, trade_price_open numeric NOT NULL, trade_price_close numeric NOT NULL, volume numeric NOT NULL, symbol text NOT NULL);
